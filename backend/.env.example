# Server Configuration
NODE_ENV=development
PORT=5100
SERVE_FRONTEND=false

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database Configuration
# Option 1: Use DATABASE_URL (full connection string)
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://root@127.0.0.1:3306/360_feedback

# Option 2: Use individual database variables (if DATABASE_URL is not set)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=360_feedback

# Production Example (commented out)
# NODE_ENV=production
# PORT=5100
# SERVE_FRONTEND=true
# JWT_SECRET=your-production-secret-here
# DATABASE_URL=mysql://username:password@production-host:3306/360_feedback
